# Phase 1 merge into main (2026-09-26)

The project owner asked to merge Phase 1 into `main` before gate G1. This deviates from plan decision I3
("merged into `main` once the measurable criteria are met"), which now records the exception: G1 stays open and
must pass before Phase 2 ships.

## Merges

Each code repository merged `feat/phase-01-clipboard` into `main` with a signed-off merge commit (no fast
forward, so the phase boundary stays visible). Checked before merging: no conflicts (`git merge-tree`); CI green
on every branch head; locally `./gradlew check` (347 unit tests), 7 Swift packages (226 tests) and SwiftLint
`--strict`, `cargo fmt`/`clippy -D warnings`/`cargo test` (32 passed, 6 database tests left to CI), every
shared checker (1128 vector checks, 18 files regenerated byte for byte, schemas, 207 catalog strings), and
the hub docs (validator `problems=0`, 67 bilingual pairs).

| Repository | Merge commit | Commits merged | Note |
|------------|--------------|----------------|------|
| handlive-shared | 9fd65cc | 40 | merged first: the platform CIs check out shared `main` |
| handlive-relay | b7b91d5 | 2 | `main` also keeps the Apache-2.0 license fix of the workspace manifest |
| handlive-android | 19f6ab9 | 84 | the CI fix cherry-picked to `main` earlier merges cleanly |
| handlive-apple | ad018be | 89 | |

The hub and `HandLive/.github` work on `main` directly. The `feat/phase-01-clipboard` branches stay on GitHub
for reference and can be deleted.

## Gate G1 — still open

- Device matrix of `shared/tools/bench/README.md` on a Pixel, a Samsung, an Apple silicon Mac and an Intel Mac:
  text < 50 ms, 5 MB image < 2 s, reconnect < 3 s, judged at the 95th percentile.
- Pairing Android ↔ Mac by QR and by PIN on real devices; Security Code identical on both.
- TalkBack and VoiceOver passes; 200 % font scale and AX5 in English and Vietnamese.
- System setting names in both languages (Android "Pause app activity if unused", OEM autostart pages; macOS
  "Paste from Other Apps" / "Always Allow").
- The Mac app built and signed with the Developer ID team (the data-protection keychain needs it).

Status: DONE_WITH_CONCERNS
Summary: Phase 1 is merged into main in handlive-shared, handlive-relay, handlive-android and handlive-apple.
Concerns/Blockers: gate G1 has not run; it needs real phones and Macs.
